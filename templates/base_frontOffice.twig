{% extends 'base.html.twig' %}

    {% block stylesheets %}
        <link href="/styles/app_frontOffice.css" rel="stylesheet">
    {% endblock %}
    {% block navbar %}{# create the header here #}{% endblock %}
    {% block sideNavbar %}{# let it empty (it's only for backOffice)#}{% endblock %}
    {% block footer %}{# create the footer here #}{% endblock %}