{% extends 'base.html.twig' %}

{% block body %}
    {% for email in emails %}
        <h3>{{ email.fullName }}</h3>
        <p>{{ email.subject }}</p>
        <p>{{ email.message }}</p>
    {% endfor %}

{% endblock %}
